import DOM;

# All functions in `toro` are curried
#
# List.map : (a -> b) -> [a] -> [b]

def main = {
  def element: Maybe(DOM.Node) = DOM.querySelector("#teste");
  
  DOM.map(DOM.setText("Hello World!"), element);
}